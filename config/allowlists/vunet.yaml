# VuNET / Martin Audio control multicast — FILL FROM CAPTURE
# See docs/capture-playbook.md (Martin Control VLAN).

name: vunet
vlan: control
groups: []
# Example after capture:
# groups:
#   - name: vunet-discovery
#     address: 239.x.x.x
#     port: 0
#     proto: udp
#     direction: both
#     notes: captured YYYY-MM-DD
