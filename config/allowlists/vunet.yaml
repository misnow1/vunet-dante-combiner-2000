# VuNET / Martin Audio — NOT loaded by the combiner.
# Clients share the Control VLAN with the amps (native L2). Do not add
# reflector groups here; allowlist vlan must be dante (see dante.yaml / shure.yaml).
# Optional capture notes: docs/capture-playbook.md and docs/protocols.md.

name: vunet
vlan: dante
groups: []
